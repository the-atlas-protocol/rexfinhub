# AGENTS.md — entry point for non-Claude agent runners

This mirrors `CLAUDE.md` at this repo root (nearest-file-wins convention — Claude Code reads `CLAUDE.md` directly; other runners read this).

Read `CLAUDE.md` first, then `docs/INDEX.md` — that routes to the right domain doc for whatever task you're doing. Do not duplicate that content here; it changes independently of this file.
