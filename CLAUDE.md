# rex-asia

You are in **rex-asia** — the Korea/Asia monthly report + data pipeline (Grace's data).
Data lives on the **D: drive** (`D:/rex-asia/...`) — never enumerate D: cache dirs; read known paths only. ALWAYS back up the DB before modifying (see `lessons_rex_asia_db`). FEPI_LN is a separate UCITS product, not a dupe. Local-only, no VPS deploy.

- **System constitution**: `C:/Projects/Library/ATLAS.md`
- **Conductor**: Hub (this domain was absorbed into Hub). **Model**: `C:/Projects/Library/model-routing.md`.
- **Foundry migration note**: slated to become `Rexfinhub/asia/` (Hub monorepo). Its D: data paths are absolute and unaffected by the code move.
- Keep this file a pointer.
