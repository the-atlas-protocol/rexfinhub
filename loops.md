# Rexfinhub/loops.md — this domain's loops

None scheduled. The data pipeline is ON-DEMAND via /refreshdata since 2026-06-17
(bloomberg/daily/preflight/gate-open timers disabled; `scripts/run_chain.py` is the one
entrypoint). Rows added here are picked up by the central runner via
`Loops/runner/manifests.txt` when the host has this repo.

## Loops

| name | purpose | cadence | cron | runner | model | prompt | output | enabled |
|---|---|---|---|---|---|---|---|---|
